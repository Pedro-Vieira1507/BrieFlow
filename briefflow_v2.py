# ARQUIVO REMOVIDO — substituído por api/main.py (FastAPI) + web/ (React/Vite)
# Este arquivo é mantido apenas para referência histórica.
# NÃO execute este arquivo.
raise SystemExit("Use: uvicorn api.main:app --reload --port 8000  e  cd web && npm run dev")
