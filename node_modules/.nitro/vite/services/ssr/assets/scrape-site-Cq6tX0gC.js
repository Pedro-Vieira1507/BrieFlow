import { d as TSS_SERVER_FUNCTION, t as createServerFn } from "./createServerFn-CIHAFgYl.js";
//#region node_modules/@tanstack/start-server-core/dist/esm/createServerRpc.js
var createServerRpc = (serverFnMeta, splitImportFn) => {
	const url = "/_serverFn/" + serverFnMeta.id;
	return Object.assign(splitImportFn, {
		url,
		serverFnMeta,
		[TSS_SERVER_FUNCTION]: true
	});
};
//#endregion
//#region src/lib/scrape-site.ts?tss-serverfn-split
function normalizeUrl(raw) {
	let value = raw.trim().replace(/[.,;:!?)]+$/, "");
	if (!value) return null;
	if (!/^https?:\/\//i.test(value)) value = `https://${value}`;
	try {
		const url = new URL(value);
		if (!["http:", "https:"].includes(url.protocol)) return null;
		return url.toString();
	} catch {
		return null;
	}
}
function decodeHtml(text) {
	return text.replace(/&nbsp;/gi, " ").replace(/&amp;/gi, "&").replace(/&lt;/gi, "<").replace(/&gt;/gi, ">").replace(/&quot;/gi, "\"").replace(/&#39;/gi, "'").replace(/&#x27;/gi, "'").replace(/&#(\d+);/g, (_, n) => String.fromCharCode(Number(n))).replace(/\s+/g, " ").trim();
}
function metaContent(html, names) {
	for (const name of names) {
		const patterns = [new RegExp(`<meta[^>]+(?:name|property)=["']${name}["'][^>]+content=["']([^"']+)["']`, "i"), new RegExp(`<meta[^>]+content=["']([^"']+)["'][^>]+(?:name|property)=["']${name}["']`, "i")];
		for (const re of patterns) {
			const m = html.match(re);
			if (m?.[1]) return decodeHtml(m[1]);
		}
	}
	return "";
}
function extractTitle(html) {
	const og = metaContent(html, ["og:title", "twitter:title"]);
	if (og) return og;
	const m = html.match(/<title[^>]*>([\s\S]*?)<\/title>/i);
	return m?.[1] ? decodeHtml(m[1]) : "";
}
function extractHeadings(html, limit = 8) {
	const headings = [];
	const re = /<h[1-3][^>]*>([\s\S]*?)<\/h[1-3]>/gi;
	let match;
	while ((match = re.exec(html)) !== null && headings.length < limit) {
		const text = decodeHtml(match[1].replace(/<[^>]+>/g, " "));
		if (text && text.length > 2 && text.length < 160) headings.push(text);
	}
	return [...new Set(headings)];
}
function extractBodySnippet(html, maxLen = 1800) {
	let cleaned = html.replace(/<script[\s\S]*?<\/script>/gi, " ").replace(/<style[\s\S]*?<\/style>/gi, " ").replace(/<noscript[\s\S]*?<\/noscript>/gi, " ").replace(/<svg[\s\S]*?<\/svg>/gi, " ").replace(/<!--[\s\S]*?-->/g, " ");
	const text = decodeHtml(((cleaned.match(/<main[\s\S]*?>([\s\S]*?)<\/main>/i) || cleaned.match(/<article[\s\S]*?>([\s\S]*?)<\/article>/i) || cleaned.match(/<body[\s\S]*?>([\s\S]*?)<\/body>/i))?.[1] ?? cleaned).replace(/<[^>]+>/g, " "));
	if (text.length <= maxLen) return text;
	return `${text.slice(0, maxLen)}…`;
}
function inferBrandName(title, url) {
	if (title) {
		const parts = title.split(/[|\-–—·:]/).map((p) => p.trim()).filter(Boolean);
		if (parts[0] && parts[0].length < 60) return parts[0];
	}
	try {
		const host = new URL(url).hostname.replace(/^www\./, "");
		const base = host.split(".")[0] ?? host;
		return base.charAt(0).toUpperCase() + base.slice(1);
	} catch {
		return "Marca";
	}
}
function parseWebsiteHtml(html, url) {
	const title = extractTitle(html);
	const description = metaContent(html, [
		"description",
		"og:description",
		"twitter:description"
	]);
	const keywords = metaContent(html, ["keywords"]);
	const ogImage = metaContent(html, ["og:image", "twitter:image"]);
	const headings = extractHeadings(html);
	const bodySnippet = extractBodySnippet(html);
	return {
		url,
		title,
		description,
		brandName: inferBrandName(title, url),
		headings,
		bodySnippet,
		ogImage: ogImage || void 0,
		keywords: keywords || void 0
	};
}
async function fetchWebsite(url) {
	const controller = new AbortController();
	const timeout = setTimeout(() => controller.abort(), 15e3);
	try {
		const res = await fetch(url, {
			signal: controller.signal,
			headers: {
				"User-Agent": "Mozilla/5.0 (compatible; BrieFlowBot/1.0; +https://brieflow.app)",
				Accept: "text/html,application/xhtml+xml"
			},
			redirect: "follow"
		});
		if (!res.ok) throw new Error(`Site retornou HTTP ${res.status}`);
		const contentType = res.headers.get("content-type") ?? "";
		if (contentType && !contentType.includes("text/html") && !contentType.includes("application/xhtml")) throw new Error("A URL não aponta para uma página HTML.");
		const html = await res.text();
		if (!html || html.length < 40) throw new Error("Página vazia ou inacessível.");
		return parseWebsiteHtml(html, url);
	} finally {
		clearTimeout(timeout);
	}
}
var scrapeWebsite_createServerFn_handler = createServerRpc({
	id: "bdfb90ff62430c1f7b847286a83a77ca7732c6b5e9ba61a16f7e09e20458552b",
	name: "scrapeWebsite",
	filename: "src/lib/scrape-site.ts"
}, (opts) => scrapeWebsite.__executeServer(opts));
var scrapeWebsite = createServerFn({ method: "POST" }).validator((data) => {
	if (!data?.url || typeof data.url !== "string") throw new Error("URL obrigatória.");
	const normalized = normalizeUrl(data.url);
	if (!normalized) throw new Error("URL inválida.");
	return { url: normalized };
}).handler(scrapeWebsite_createServerFn_handler, async ({ data }) => {
	return fetchWebsite(data.url);
});
//#endregion
export { scrapeWebsite_createServerFn_handler };
